# Lockdown and tokens

Parent scope: harden hostahtml for Codurance-only uploads (server-side identity checks), fix S3 uploads bucket lifecycle/versioning story, narrow GitHub deploy IAM, document app-level share tokens (ADR), repo hygiene (tsc/CI/gitignore), and prior-review API/frontend hardening (payload hint, HTML sniff, JSON headers, S3 key entropy, JWT decode).

Source: agent execution plan “Lockdown and tokens” (do not edit that plan file from issues).

Issue **05** (app-level share tokens phase 1) is **`ready-for-human`**: run a planning session before implementation; ADR from issue **04** is still a prerequisite.
